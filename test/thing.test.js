import Thing from '../src/thing.js';

describe('Thing', () => {
    describe('It does things', () => {
        it('Does the thing', () => {
            expect(true).toBe(true);
        });
    });
});
